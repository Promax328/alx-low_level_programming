#include "main.h"

/**
 * _abs - checks the absolute value of a number
 * @c: number to get the absolute value
 *
 * Return: the absolute value of c
 */

int _abs(int c)
{
	if (c >= 0)
		return (c);

	else
		return (-c);

}
