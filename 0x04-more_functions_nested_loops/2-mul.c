#include "main.h"

/**
 * mul - returns the product of two digit
 * @a: first digit to be multiplied
 * @b: second digit to be multiplied
 *
 * Return: the product
 */

int mul(int a, int b)
{
	return (a * b);
}
